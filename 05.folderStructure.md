## Folder Structure

- AWS configure in home directory → `~/.aws/credentials`
- Each folder knows on which account resources should be created.

```
terraform/
    |--- main.tf
    |--- variables.tf
    |--- outputs.tf
    |--- terraform.tfvars
    |--- .gitignore
```

---
